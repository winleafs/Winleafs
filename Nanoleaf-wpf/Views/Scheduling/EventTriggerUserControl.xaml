<UserControl x:Class="Winleafs.Wpf.Views.Scheduling.EventTriggerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="78" d:DesignWidth="220">
    <Grid>
        <Label Content="{Binding TriggerType}" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="White" Height="26" Width="195"/>
        <Label Content="{Binding Description}" VerticalAlignment="Top" Foreground="White" Height="26" Margin="0,26,0,0"/>
        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Height="25" Click="Delete_Click" Margin="0,1,0,0">
            <Image Source="../../Icons/appbar.delete.png"></Image>
        </Button>
        <Label Content="{Binding Brightness}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Foreground="White" Height="26" Width="30" Margin="0,52,0,0"/>
        <Label Content="{Binding EffectName}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Foreground="White" Height="26" Width="175" Margin="0,52,0,0"/>
        <Border BorderThickness="0 1 0 0" BorderBrush="White"/>
    </Grid>
</UserControl>
