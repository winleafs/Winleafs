<Window x:Class="Winleafs.Wpf.Views.Scheduling.AddTimeTriggerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Winleafs.Wpf.Views.Scheduling"
        xmlns:effects="clr-namespace:Winleafs.Wpf.Views.Effects"
        mc:Ignorable="d"
        Title="{x:Static local:Resources.AddTriggerTitle}" Height="320" Width="320" ResizeMode="NoResize">
    <Grid Background="{StaticResource NanoleafBlack}">
        <local:TimeComponentUserControl x:Name="TimeComponent" Width="300" Height="150" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <Button Content="{x:Static local:Resources.Cancel}" HorizontalAlignment="Left" Margin="10,0,0,10" Width="84" Background="{StaticResource NanoleafGreen}" Foreground="White" BorderBrush="{x:Null}" Click="Cancel_Click" Height="27" VerticalAlignment="Bottom"/>
        <Button Content="{x:Static local:Resources.Add}" Margin="0,0,10,10" Background="{StaticResource NanoleafGreen}" Foreground="White" BorderBrush="{x:Null}" Click="Add_Click" Height="27" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
        <effects:EffectComboBox x:Name="EffectComboBox" HorizontalAlignment="Left" Margin="13,156,0,0" VerticalAlignment="Top" Width="273" Height="45"/>
        <Label Content="{x:Static local:Resources.Brightness}" ContentStringFormat="{}{0}:" HorizontalAlignment="Left" Margin="10,208,0,0" VerticalAlignment="Top" Foreground="White"/>
        <Label x:Name="BrightnessLabel" Content="0" HorizontalAlignment="Left" Margin="346,208,0,0" VerticalAlignment="Top" Foreground="White"/>
        <Slider HorizontalAlignment="Left" Margin="91,211,0,0" VerticalAlignment="Top" Width="195" Minimum="0" Maximum="100" Value="{Binding Brightness}" IsSnapToTickEnabled="True" TickFrequency="5"/>
    </Grid>
</Window>
